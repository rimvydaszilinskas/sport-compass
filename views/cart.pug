extends layout/index
block content
	#container.container-fluid
		.container
			.row
				.col-md-4
					h2 Cart
			hr
			.container-fluid
				.row
					if (computers.length === 0)
						.col-md-4.offset-md-4
							h4="No items in the basket"
					else 
						each computer in computers
							.media
								img.mr-3(src=`/images/${computer.image}`, alt='image-item', style='max-height: 128px')
								.media-body
									h5.mt-0=`${computer.manufacturer} ${computer.model}`
									p=`${computer.processor} | ${computer.graphics} | ${computer.memory}`
									p=`${computer.price} DKK.`
			hr
			.row
				.col-md-4.offset-md-9
					| Total value: 
					span 75000 DKK
					button.btn.btn-success Checkout
